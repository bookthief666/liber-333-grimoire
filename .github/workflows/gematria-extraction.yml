name: Apply Gematria extraction

on:
  push:
    branches:
      - agent/liber333-gematria-engine

permissions:
  contents: write

jobs:
  transform-test-build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply deterministic extraction
        run: node scripts/apply-gematria-extraction.mjs

      - name: Install dependencies
        run: npm ci

      - name: Run Gematria regression tests
        run: npm run test:gematria

      - name: Build
        run: npm run build

      - name: Commit generated extraction
        shell: bash
        run: |
          if git diff --quiet && git diff --cached --quiet; then
            echo "Gematria extraction is already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "refactor: extract Gematria engine with regression tests"
          git push origin HEAD:${GITHUB_REF_NAME}
